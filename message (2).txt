# 📋 PR #17 – Checklist Review

## 🔴 P0 – BLOCKER (bắt buộc fix trước khi merge)

**1. Fix Prompt Injection Detection**
- Thêm **guard chạy TRƯỚC mọi heuristic** intent matching.
- Pattern tối thiểu: `ignore previous instructions`, `disregard previous instructions`, `you are now`, `forget your instructions` + biến thể tiếng Việt.
- Output bắt buộc khi phát hiện injection:
  - `intent = UNKNOWN`, confidence thấp (`&lt;0.1`)
  - `tool_calls = []`
  - `needs_confirm = true`, `needs_clarification = true`
- ➕ **Unit test riêng cho `EDGE_CASE_001`** – phải **fail độc lập** khi regression, KHÔNG dựa vào aggregate accuracy.

**2. Gỡ module Frozen khỏi PR (`internal/pipeline/`, `internal/llm/`)**
- `ACTIVE_MODULES.md` đánh dấu `internal/pipeline/` là **Frozen** → PR đang thêm `internal/pipeline/stages/...` là **xung đột scope trực tiếp**.
- `internal/llm/` không nằm trong active module + phụ thuộc `pipeline/stages`.
- **Cách xử lý:** Gỡ cả hai khỏi PR. Logic LLM cần thiết → gộp vào `internal/providers/` (đã được phép). Chỉ giữ approval nếu **lead đã duyệt rõ ràng** và ghi vào PR description.

**3. Chọn 1 classifier duy nhất (xóa bản trùng)**
- Hiện có **2 implementation song song**:
  - ✅ Giữ: `internal/agent/intent/...` (đúng ownership theo `ACTIVE_MODULES.md`)
  - ❌ Xóa: `internal/agent/intent_classifier.go`, `tool_registry.go`, `types.go`, `config.go`, `confidence.go`
- Đồng bộ **một bộ threshold duy nhất**, đặc biệt `AmbiguousLow` (đang lệch `0.50` vs `0.60`).

**4. Safety test phải fail độc lập (không cho aggregate che lỗi)**
- `tests/safety/intent_eval_test.go` hiện chỉ fail khi accuracy tổng `&lt;80%` → che mất lỗi an toàn.
- Tách **hard assertion riêng** cho từng case safety-critical (mỗi case fail là cả test fail):
  - Prompt injection → pass tuyệt đối
  - Dangerous action **không** được phân loại thành `safe_read`
  - Missing param của dangerous action → phải hỏi lại
  - Tool nguy hiểm → `needs_confirm = true`

---

## 🟠 P1 – Contract &amp; Safety (nên fix trước merge)

**5. Đồng bộ tool name theo `03-contracts.md`**
- Contract yêu cầu dạng `<domain>.<action>`: `gmail.listEmails`, `gmail.sendEmail`, `calendar.createEvent`, `sandbox.runPython`, `sandbox.runShell`.
- Không để tên phẳng (`send_email`, `exec`) đi qua boundary tool call.
- Alias nội bộ phải **normalize về tên contract** trước khi tạo `ToolCall` + thêm test chống drift.

**6. Đồng bộ `RiskDecision` contract**
- `blocked` là **decision/status**, KHÔNG phải `RiskLevel`.
- Output risk tối thiểu phải có: `toolCallId`, `toolName`, `riskLevel`, `decision`, `requiresApproval`, `reason`, `checkedAt`.
- Nếu chưa làm full approval flow → giữ nhỏ và ghi rõ là *internal helper*, không claim "maps to RiskDecision".

**7. Đảm bảo dangerous/composite KHÔNG tự thực thi**
- Intent classifier chỉ **đề xuất** intent/tool call.
- Không path nào được execute tool khi chưa qua `RiskDecision` + approval.
- Thêm test invariant: send email / delete file / shell / python / composite có bước nguy hiểm → **requires approval**.

---

## 🟡 P2 – Scope &amp; Housekeeping

**8. Rà lại `internal/memory/`** – short-term memory thuộc **Sprint 2**. PR Sprint 1 G3 chỉ giữ phần chứng minh "không bị ảnh hưởng bởi memory cũ", không implement subsystem rộng.

**9. Bỏ generated artifact** – không commit `evaluation_results.json`, thêm `.gitignore`. Kết quả để trong PR comment / CI artifact.

**10. Sửa dataset path test** – `intent_eval_dataset.json` đang đọc bằng relative path → đảm bảo chạy được từ repo root lẫn package dir. Chạy `go test ./...` sau khi apply.

**11. Không hard-code path/heading [SOUL.md](http://soul.md/)** – `os.ReadFile("configs/SOUL.md")` dễ vỡ; heading `## 🔴 LUẬT SINH TỒN` là dependency ngầm. Dùng `go:embed` hoặc inject qua config; marker ổn định, không phụ thuộc emoji.

**12. Tách báo cáo Heuristic vs LLM** – 95.16% hiện là **heuristic mặc định**, chưa chứng minh LLM path. PR/README ghi rõ: heuristic accuracy / LLM accuracy (nếu đã chạy) / dataset / command. **Không claim** đạt KPI bằng LLM nếu chưa chạy thật.

**13. Làm rõ LLM provider mặc định** – `gemini-1.5-flash` đang hard-code → đưa qua config. Nếu thêm OpenAI dependency mà chưa dùng → gỡ khỏi `go.mod`.

**14. Giải thích thay đổi vùng Shared** – `cmd/`, `configs/`, `tests/`, `go.mod/go.sum` cần nêu rõ trong PR description. `cmd/evaluate` phải là entrypoint mỏng, không chứa business logic nặng.

---

## ✅ Định nghĩa "Tối thiểu để MERGE"

- [ ] 🔴 Prompt injection: hard-fail test riêng + đã fix
- [ ] 🔴 Gỡ `internal/pipeline/` &amp; `internal/llm/` (hoặc có approval scope rõ ràng)
- [ ] 🔴 Chỉ còn **1** classifier implementation
- [ ] 🟠 Tool name normalize theo `03-contracts.md`
- [ ] 🟡 Gỡ `evaluation_results.json`
- [ ] 🟠 Có safety/contract test tách riêng
- [ ] 🟡 Đã chạy &amp; ghi lại kết quả `go test ./...`

&gt; 🎯 **Mục tiêu:** Đưa PR về đúng phạm vi **Sprint 1 – G3**: *intent classification tối giản, có safety guardrails, không drift contract, không mở rộng kiến trúc ngoài roadmap.*