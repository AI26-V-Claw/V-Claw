# backup (Sprint 3)

Local backup and restore workflows live here.

V-Claw should be able to back up PostgreSQL data, config, audit receipts, provider metadata, and workspace indexes.

Keep this in the skeleton, but do not build it before the core assistant,
providers, tools, approvals, and audit path are stable.
