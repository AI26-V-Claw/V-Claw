# V-Claw Sandbox - Python Dependencies
# Sprint 1: Thư viện cơ bản
# Sprint 2: Thêm office automation libraries (pandas, openpyxl, python-docx)

# ─── Data processing ──────────────────────────────────────────────────────────
pandas==2.2.3
numpy==2.2.3

# ─── Excel support ────────────────────────────────────────────────────────────
openpyxl==3.1.5
xlrd==2.0.1

# ─── Word document support ────────────────────────────────────────────────────
python-docx==1.1.2

# ─── CSV / text utils ─────────────────────────────────────────────────────────
chardet==5.2.0

# ─── Date/time utils ──────────────────────────────────────────────────────────
python-dateutil==2.9.0.post0

# ─── JSON/YAML (stdlib json đủ dùng, yaml cho config đọc) ────────────────────
PyYAML==6.0.2

# ─── Path utilities ───────────────────────────────────────────────────────────
pathlib2==2.3.7.post1

# ─── Hashing & checksum ───────────────────────────────────────────────────────
# (dùng hashlib stdlib, không cần thêm)

# ─── Không bao gồm ────────────────────────────────────────────────────────────
# requests / httpx / aiohttp  -> cấm network trong sandbox
# subprocess / os (stdlib)    -> chỉ sandbox runner được gọi
# paramiko / fabric           -> SSH bị cấm
