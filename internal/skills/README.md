# skills

Runtime loading, parsing, searching, and enabling of V-Claw skills live here.

The root `skills/` folder stores skill content. This `internal/skills` module implements the loader/search/runtime behavior.

