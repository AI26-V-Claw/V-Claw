# pipeline

Pluggable agent execution stages live here.

The pipeline should keep the assistant flow explicit: context, intent, plan, provider selection, policy, tool execution, observation, memory, and finalization.

