# tools

Agent tools live here. All tools should be registered through a shared registry and gated by policy, approval, and audit.

Planned groups:

- `office`: Google Workspace-facing tools.
- `web`: public web search and fetch tools.
- `os`: local filesystem, shell, Python, and desktop tools.
- `memory`: user memory tools.
- `scheduler`: reminders and recurring jobs.

