services:
  vclaw:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["bot"]
    env_file:
      - .env
    environment:
      VCLAW_HTTP_ADDR: ":8080"
      VCLAW_APPROVAL_MODE: "telegram"
    ports:
      - "8080:8080"
    restart: unless-stopped
